{
  "title": "Gitleaks Cloud — GitHub API Key Hunter — Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "search_for": {
      "title": "What are you hunting for?",
      "description": "Pick how you want to define the secret. 'Known platform' = use our hand-tuned rules (best accuracy). 'Keyword' = just type a word like 'anthropic', we auto-generate UPPER/snake/camel variants for you (no regex needed). 'Custom regex' = power user, paste your own pattern.",
      "type": "string",
      "editor": "select",
      "default": "platform",
      "enum": ["platform", "keyword", "regex"],
      "enumTitles": [
        "A known platform (pick from our list of 30)",
        "A keyword (just type a word, we expand the variations)",
        "A custom regex (you write the exact pattern)"
      ],
      "sectionCaption": "What to search for"
    },
    "platform": {
      "title": "Platform",
      "description": "Used only when 'What are you hunting for?' = 'A known platform'. Pick the service whose credentials you want to find. 'Other' falls back to broad detection across all 30 services.",
      "type": "string",
      "editor": "select",
      "default": "razorpay",
      "enum": [
        "razorpay", "stripe", "aws", "gcp-api-key", "gcp-service-account",
        "openai", "anthropic", "groq", "gemini", "github-pat",
        "supabase", "firebase", "clerk", "sendgrid", "mailgun",
        "postmark", "twilio", "slack-bot", "slack-webhook", "discord-bot",
        "datadog", "pagerduty", "mongodb-uri", "postgres-uri", "redis-uri",
        "payu", "cashfree", "surepass", "decentro", "karza", "attestr", "tartan",
        "evm-private-key", "solana-private-key", "bitcoin-private-key", "bip39-mnemonic",
        "firecrawl", "trigger-dev", "jwt-generic", "rsa-private-key",
        "custom"
      ],
      "enumTitles": [
        "Razorpay", "Stripe", "AWS", "Google Cloud API key", "Google Cloud service account",
        "OpenAI", "Anthropic Claude", "Groq", "Google Gemini", "GitHub PAT",
        "Supabase", "Firebase", "Clerk", "SendGrid", "Mailgun",
        "Postmark", "Twilio", "Slack bot token", "Slack webhook URL", "Discord bot token",
        "Datadog", "PagerDuty", "MongoDB connection URI", "Postgres connection URI", "Redis connection URI",
        "PayU (India)", "Cashfree Payments (India)", "Surepass (India KYC)", "Decentro (India BaaS)", "Karza / Perfios (India KYC)", "Attestr (India KYC)", "Tartan (India KYC)",
        "EVM Private Key (Ethereum / Polygon / BSC / Tron)", "Solana Private Key / Keypair", "Bitcoin Private Key (WIF)", "BIP39 Mnemonic / Seed Phrase",
        "Firecrawl", "Trigger.dev", "Generic JWT", "RSA private key",
        "Other — type the service name in the next field"
      ]
    },
    "platform_custom": {
      "title": "Custom platform name (only if you picked 'Other' above)",
      "description": "Free-text label for unlisted services. Used as the display name in your output. Detection falls back to all 30 ruleset patterns merged together — gives broad coverage when we don't have a hand-tuned rule.",
      "type": "string",
      "editor": "textfield",
      "nullable": true,
      "example": "convex"
    },
    "additional_platforms": {
      "title": "Also scan for these platforms (optional, multi-select)",
      "description": "Used only in 'A known platform' mode. Pick more services to scan alongside the primary platform — useful for one-pass org audits. Each is still priced as one repo, not one per platform.",
      "type": "array",
      "editor": "select",
      "items": {
        "type": "string",
        "enum": [
          "razorpay", "stripe", "aws", "gcp-api-key", "gcp-service-account",
          "openai", "anthropic", "groq", "gemini", "github-pat",
          "supabase", "firebase", "clerk", "sendgrid", "mailgun",
          "postmark", "twilio", "slack-bot", "slack-webhook", "discord-bot",
          "datadog", "pagerduty", "mongodb-uri", "postgres-uri", "redis-uri",
          "payu", "cashfree", "surepass", "decentro", "karza", "attestr", "tartan",
          "evm-private-key", "solana-private-key", "bitcoin-private-key", "bip39-mnemonic",
          "firecrawl", "trigger-dev", "jwt-generic", "rsa-private-key"
        ],
        "enumTitles": [
          "Razorpay", "Stripe", "AWS", "GCP API key", "GCP service account",
          "OpenAI", "Anthropic Claude", "Groq", "Gemini", "GitHub PAT",
          "Supabase", "Firebase", "Clerk", "SendGrid", "Mailgun",
          "Postmark", "Twilio", "Slack bot", "Slack webhook", "Discord bot",
          "Datadog", "PagerDuty", "MongoDB URI", "Postgres URI", "Redis URI",
          "PayU", "Cashfree", "Surepass", "Decentro", "Karza/Perfios", "Attestr", "Tartan",
          "EVM private key", "Solana keypair", "Bitcoin WIF", "BIP39 mnemonic",
          "Firecrawl", "Trigger.dev", "Generic JWT", "RSA private key"
        ]
      },
      "uniqueItems": true,
      "nullable": true
    },
    "keyword": {
      "title": "Keyword (only if you picked 'A keyword' above)",
      "description": "Type one word — usually a service name or variable name. We auto-generate ~15 common patterns from it: ANTHROPIC_API_KEY, anthropic_api_key, anthropicApiKey, ANTHROPIC_SECRET, ANTHROPIC_TOKEN, etc. No regex knowledge required.",
      "type": "string",
      "editor": "textfield",
      "nullable": true,
      "example": "anthropic"
    },
    "regex_pattern": {
      "title": "Custom regex (only if you picked 'A custom regex' above)",
      "description": "Power-user mode. Provide a regex that matches the secret value. The literal/fixed portion is used as the GitHub Code Search query; the full regex extracts the actual secret value from matching files. Examples: rzp_live_[A-Za-z0-9]{14} or sk-ant-api03-[A-Za-z0-9_-]{93}",
      "type": "string",
      "editor": "textfield",
      "nullable": true,
      "example": "rzp_live_[A-Za-z0-9]{14}"
    },
    "scope": {
      "title": "Where on GitHub to search",
      "description": "All of GitHub = search across every public repo (fast Code Search if you provide a PAT below, otherwise we search and clone top matches). User or organization = scan every repo owned by a user/org (deep, scans full git history). Single repo = scan one specific repo (deep, scans full git history).",
      "type": "string",
      "editor": "select",
      "default": "all_github",
      "enum": ["all_github", "user_or_org", "single_repo"],
      "enumTitles": [
        "All of GitHub (search every public repo)",
        "A specific user or organization (scan all their repos)",
        "A single repository (scan one repo URL)"
      ],
      "sectionCaption": "Where to search"
    },
    "target": {
      "title": "Target (user, organization, or repo URL)",
      "description": "Only used if 'Where on GitHub to search' is 'A user/org' or 'A single repo'. Accepts: a username like 'octocat', an org name like 'microsoft', or a full repo URL like 'https://github.com/owner/name'. Leave blank for 'All of GitHub' mode.",
      "type": "string",
      "editor": "textfield",
      "nullable": true,
      "example": "https://github.com/owner/repo"
    },
    "github_pat": {
      "title": "GitHub Personal Access Token (highly recommended)",
      "description": "Optional, but recommended. Without a PAT: GitHub gives ~10 search requests/minute, no Code Search, and no private repos. With a PAT: 30 search/min + 5000 core/hour, Code Search unlocks (10× faster, 50× more coverage), private repos accessible. Generate one at https://github.com/settings/tokens — 'public_repo' scope is enough for public scans, 'repo' for private. Stored encrypted by Apify, never logged.",
      "type": "string",
      "editor": "textfield",
      "isSecret": true,
      "nullable": true,
      "sectionCaption": "GitHub authentication (recommended)"
    },
    "max_results": {
      "title": "Max results",
      "description": "Hard cap on output. In fast Code Search mode = max findings to return (1-1000, GitHub's limit). In clone mode = max repos to scan. Higher = more coverage but slower and costs more. 100 is a good starting point.",
      "type": "integer",
      "editor": "number",
      "default": 100,
      "minimum": 1,
      "maximum": 1000,
      "sectionCaption": "Limits and filters"
    },
    "pushed_after": {
      "title": "Only repos pushed AFTER this date (optional)",
      "description": "Date filter for repo discovery. Only include repos with last push on or after this date. Format: YYYY-MM-DD. Use to focus on fresh leaks.",
      "type": "string",
      "editor": "datepicker",
      "nullable": true,
      "example": "2025-01-01"
    },
    "pushed_before": {
      "title": "Only repos pushed BEFORE this date (optional, goldmine filter)",
      "description": "The goldmine filter. Set this to 1-2 years ago + Maximum stars = 5 → catches forgotten amateur repos where keys were never rotated and nobody else is looking. Format: YYYY-MM-DD.",
      "type": "string",
      "editor": "datepicker",
      "nullable": true,
      "example": "2024-01-01"
    },
    "language": {
      "title": "Programming language (optional)",
      "description": "Narrow to repos primarily in this language. Leave blank for all languages. Common values: javascript, typescript, python, go, php, java, ruby.",
      "type": "string",
      "editor": "textfield",
      "nullable": true,
      "example": "javascript"
    },
    "min_stars": {
      "title": "Minimum stars (optional)",
      "description": "Only consider repos with at least N stars. Use min_stars=100 to focus on prominent production repos.",
      "type": "integer",
      "editor": "number",
      "nullable": true,
      "minimum": 0
    },
    "max_stars": {
      "title": "Maximum stars (optional, pair with pushed_before for the goldmine play)",
      "description": "Only consider repos with at most N stars. Use max_stars=5 with pushed_before=2 years ago to target low-attention forgotten repos — much higher hit rate of unrotated keys than scanning popular repos.",
      "type": "integer",
      "editor": "number",
      "nullable": true,
      "minimum": 0
    },
    "include_extensions": {
      "title": "Only scan these file types (optional)",
      "description": "Whitelist of file extensions or filenames. Examples: .env, .yml, .json, Dockerfile. When set, findings in other files are filtered out. Empty = scan everything.",
      "type": "array",
      "editor": "stringList",
      "items": {"type": "string"},
      "uniqueItems": true,
      "nullable": true
    },
    "include_test_keys": {
      "title": "Include test/sandbox keys in output",
      "description": "Test-mode keys (rzp_test_*, sk_test_*, pk_test_*) are public by design and harmless if leaked. Default ON for completeness. Uncheck to filter them out and only show real (live) keys.",
      "type": "boolean",
      "editor": "checkbox",
      "default": true
    },
    "include_pr_refs": {
      "title": "Also scan PR branches (refs/pull/*)",
      "description": "Catches secrets that were pushed to a Pull Request and then squash-merged away — the PR head ref still holds them. Default ON. Slight slowdown per repo.",
      "type": "boolean",
      "editor": "checkbox",
      "default": true,
      "sectionCaption": "Advanced — scan depth & filters"
    },
    "include_all_branches": {
      "title": "Scan ALL branches, not just default",
      "description": "Clones with --no-single-branch so feature branches are also scanned. Default ON.",
      "type": "boolean",
      "editor": "checkbox",
      "default": true
    },
    "include_submodules": {
      "title": "Recurse into git submodules",
      "description": "Adds --recurse-submodules. Catches nested repos with their own .env files. Off by default — can be very slow on big projects.",
      "type": "boolean",
      "editor": "checkbox",
      "default": false
    },
    "include_dangling_objects": {
      "title": "Also scan dangling commits (rebased-away history)",
      "description": "Uses git fsck --dangling to find commits the developer tried to delete via rebase but didn't garbage-collect. Off by default — adds ~30s per repo.",
      "type": "boolean",
      "editor": "checkbox",
      "default": false
    },
    "commit_since": {
      "title": "Only commits after this date (optional)",
      "description": "Filter on commit date, not repo discovery date. Format YYYY-MM-DD. Catches only recently-introduced leaks.",
      "type": "string",
      "editor": "datepicker",
      "nullable": true
    },
    "commit_until": {
      "title": "Only commits before this date (optional)",
      "description": "Filter on commit date. Pair with commit_since for a date window.",
      "type": "string",
      "editor": "datepicker",
      "nullable": true
    },
    "commit_author": {
      "title": "Only commits by this author (regex, optional)",
      "description": "Filter by author name/email. Example: 'alice' or '.*@example\\.com'. Useful when auditing a specific developer.",
      "type": "string",
      "editor": "textfield",
      "nullable": true
    },
    "commit_message_grep": {
      "title": "Only commits whose message matches (regex, optional)",
      "description": "Filter by commit message regex. Example: 'wip|temp|test|hack' to surface sloppy commits where leaks usually slip in.",
      "type": "string",
      "editor": "textfield",
      "nullable": true
    },
    "commit_introduced_string": {
      "title": "Only commits that INTRODUCED this string (pickaxe, optional)",
      "description": "Fastest historical search possible. Example: 'rzp_live_' finds only commits where this string first appeared anywhere. Massive speedup vs full history scan.",
      "type": "string",
      "editor": "textfield",
      "nullable": true
    },
    "max_file_size_mb": {
      "title": "Skip files larger than (MB)",
      "description": "Avoid scanning huge binaries. Default 100 MB. Lower = faster scan, may miss leaks in big config dumps.",
      "type": "integer",
      "editor": "number",
      "default": 100,
      "minimum": 1,
      "maximum": 500
    }
  },
  "required": ["search_for", "scope"]
}
